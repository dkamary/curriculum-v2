{# Model-1 #}
<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Curriculum Vitae</title>
    <link rel="preconnect" href="https://fonts.gstatic.com" media="all">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;1,100;1,300;1,400;1,500;1,700&display=swap" rel="stylesheet" media="all">
    <link rel="stylesheet" href="{{ asset('assets/model1/css/all.min.css') }}" media="all">
    <style media="all">
        {% include 'cv/model1.css' %}
    </style>
</head>

<body>
    <div class="container">
        <aside class="sidebar-left">
            <header>
                <h1>{{ user.firstname ~ '<br>' ~ user.lastname }}</h1>
                <div class="photo-container">
                    <img class="photo" src="https://fakeimg.pl/250x250/" alt="{{ user.firstname }}">
                </div>
            </header>

            <section>
                <h2 class="margin-top">Détails personnels</h2>
                <hr>
                <p>
                    <i class="fas fa-user"></i>&nbsp;&nbsp;
                    <strong>{{ user.firstname ~ ' ' ~ user.lastname }}</strong>
                </p>
                <p>
                    <i class="fas fa-envelope"></i>&nbsp;&nbsp;
                    {{ user.email }}
                </p>
                {# <p>
                    <i class="fas fa-phone"></i>&nbsp;&nbsp;
                    {{ user.tel }}
                </p> #}
                <p>
                    <i class="fas fa-home"></i>&nbsp;&nbsp;
                    {{ user.address ~ ' ' . user.zipcode . ' ' . user.town . ' ' . user.country.name }}
                </p>
                <p>
                    <i class="fas fa-flag"></i>&nbsp;&nbsp;
                    {{ user.nationality.name }}
                </p>
                {# <p>
                    <i class="fas fa-ring"></i>&nbsp;&nbsp;
                    {{ $perso['civilite'] }}
                </p> #}
            </section>

            <section>
                <h2>Langues</h2>
                <hr>
                {% for lang in user.languageKnowledges  %}
                    <p class="d-flex justify-content-space-between">
                        <b>{{ lang.language.name|title }}</b>
                        <em>{{ lang.level.name|title }}</em>
                    </p>
                {% endfor %}
            </section>

            {# <section>
                <h2>Qualités</h2>
                <hr>
                <ul>
                    <?php foreach ($qualifications['Qualités'] as $quality => $value) : }}
                        <li><i class="fas fa-square"></i>&nbsp;&nbsp;{{ $quality }}</li>
                    <?php endforeach; }}
                    <?php unset($qualifications['Qualités']); }}
                </ul>
            </section> #}

            {# <section>
                <h2>Centre d'intérêts</h2>
                <hr>
                <ul>
                    <?php foreach ($interets as $item) : }}
                        <li><i class="fas fa-square"></i>&nbsp;&nbsp;{{ $item }}</li>
                    <?php endforeach; }}
                </ul>
            </section> #}

            <footer>
                &nbsp;
            </footer>
        </aside>
        <section class="sidebar-right">

            <section>
                <h2>Profil</h2>
                <hr>
                <p style="padding-top: 10px;"><em>{{ '' }}</em></p>
            </section>

            <section>
                <h2>Expériences Professionelles</h2>
                <hr>
                <?php foreach ($experiences as $exp) : extract($exp); }}
                    <table class="item">
                        <tbody>
                            <tr>
                                <td>
                                    <h3 class="title">{{ $poste }}</h3>
                                </td>
                                <td class="time">
                                    {{ $debut }} - {{ $present ? 'Présent' : $fin }}
                                </td>
                            </tr>
                            <tr>
                                <td colspan="2">
                                    <h4 class="subtitle">{{ $employeur }}, {{ $ville }}</h4>
                                </td>
                            </tr>
                            <tr>
                                <td colspan="2">
                                    <div class="description">
                                        {{ $description }}
                                    </div>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                <?php endforeach; }}
            </section>

            <section>
                <h2>Formations</h2>
                <hr>
                <?php foreach ($formations as $form) : extract($form); }}
                    <table class="item">
                        <tbody>
                            <tr>
                                <td>
                                    <h3 class="title">{{ $formation }}</h3>
                                </td>
                                <td class="time">
                                    {{ $debut }} - {{ $present ? 'Présent' : $fin }}
                                </td>
                            </tr>
                            <tr>
                                <td colspan="2">
                                    <h4 class="subtitle">{{ $etablissement }}, {{ $ville }}</h4>
                                </td>
                            </tr>
                            <tr>
                                <td colspan="2">
                                    <div class="description">
                                        {{ $description }}
                                    </div>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                <?php endforeach; }}
            </section>

            <section>
                <h2>Compétences</h2>
                <hr>
                <table class="item competences">
                    <tbody>
                        <?php foreach ($qualifications as $title => $list) : }}
                            <?php if (count($list) > 0) : }}
                                <tr class="row">
                                    <td width="40%"><b>{{ $title }}:</b></td>
                                    <td width="60%">{{ implode(', ', array_keys($list)) }}.</td>
                                </tr>
                            <?php endif; }}
                        <?php endforeach; }}
                    </tbody>
                </table>
            </section>

        </section>
    </div>
</body>

</html>